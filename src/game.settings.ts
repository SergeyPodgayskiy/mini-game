// Every item of array represents a card with specified value
export type GameRound = number[];

const gameRounds: GameRound[] = [
  [10, 20],
  [50, 100],
  [150, 200],
];

export default gameRounds;
